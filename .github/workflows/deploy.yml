name: Bootstrap and Deploy to Jetson

on:
  push:
    branches:
      - main

jobs:
  bootstrap-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Set up SSH agent
      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.JETSON_SSH_KEY }}

      # Step 3: Bootstrap Jetson - install prerequisites & ROS2
      - name: Bootstrap Jetson
        run: |
          ssh -A -o StrictHostKeyChecking=no -o ServerAliveInterval=60 -o ServerAliveCountMax=5 ${{ secrets.JETSON_USER }}@${{ secrets.JETSON_HOST }} << 'EOF'
            echo "Starting bootstrap..."

            # Update and upgrade
            sudo apt update -y && sudo apt upgrade -y

            # Install basic dependencies
            sudo apt install -y python3-venv python3-pip git wget lsb-release curl gnupg2 software-properties-common

            # Install ROS2 Humble if missing
            if [ ! -f /opt/ros/humble/setup.bash ]; then
              sudo curl -sSL https://raw.githubusercontent.com/ros/rosdistro/master/ros.asc | sudo apt-key add -
              sudo sh -c 'echo "deb http://packages.ros.org/ros2/ubuntu $(lsb_release -cs) main" > /etc/apt/sources.list.d/ros2.list'
              sudo apt update
              sudo apt install -y ros-humble-desktop
            fi

            # Create Python virtual environment if missing
            if [ ! -d ~/ros_env ]; then
              python3 -m venv ~/ros_env
            fi

            echo "Bootstrap complete!"
          EOF

      # Step 4: Deploy repository and build ROS2 workspace
      - name: Deploy to Jetson
        run: |
          ssh -A -o StrictHostKeyChecking=no -o ServerAliveInterval=60 -o ServerAliveCountMax=5 ${{ secrets.JETSON_USER }}@${{ secrets.JETSON_HOST }} << 'EOF'
            echo "Starting deployment..."

            # Ensure .local/bin is in PATH for pip-installed tools
            export PATH=$HOME/.local/bin:$PATH

            # Activate ROS2 virtual environment
            if [ -d ~/ros_env ]; then
              source ~/ros_env/bin/activate
            fi

            pip install --user -U pip setuptools colcon-common-extensions

            # Clone or update repository via SSH using forwarded agent
            if [ ! -d ~/Artemis6 ]; then
              git clone git@github.com:coleaskey32/Artemis6.git ~/Artemis6
            else
              cd ~/Artemis6
              git reset --hard
              git pull origin main
            fi

            # Build ROS2 workspace
            cd ~/Artemis6
            colcon build

            echo "Deployment complete!"
          EOF